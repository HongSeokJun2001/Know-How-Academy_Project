<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="reservationMapper">
 
 	<resultMap id="reservationResultSet"
           type="com.kh.know_how.reservation.model.vo.Reservation">

	    <id column="RESERVATION_NO" property="reservationNo"/>
		
		<!-- 기존 예약 정보 테이블 컬럼 -->
	    <result column="STUDENT_NO" property="studentNo"/>
	    <result column="CATEGORY_NO" property="categoryNo"/>
	    <result column="CONSULT_DATE" property="consultDate"/>
	    <result column="INQUIRY_CONTENT" property="inquiryContent"/>
	    <result column="RESERVATION_STATUS" property="reservationStatus"/>
	    <result column="CREATED_AT" property="createdAt"/>
	    <result column="UPDATED_AT" property="updatedAt"/>
	    <result column="COUNSEL_NO" property="counselNo"/>
		
		<!-- 조인으로 가져오는 추가 정보 (상담자명, 상담유형명) -->
	    <result column="COUNSELOR_NAME" property="counselorName"/>
	    <result column="CATEGORY_NAME" property="categoryName"/>
	    
	    <!-- 학생 연락처 정보 -->
	    <result column="PHONE" property="phone"/>
	    <result column="EMAIL" property="email"/>
	    
	    <!-- 학생 인적 정보 (이름, 반) -->
	    <result column="STUDENT_NAME" property="studentName"/>
	    <result column="CLASS_NAME" property="className"/>

	</resultMap>
	
	<!-- 상담 카테고리 정보 -->
	<resultMap id="categoryResultSet" type="category">
		<id column="CATEGORY_NO" property="categoryNo"/>
		<result column="CATEGORY_NAME" property="categoryName"/>
	</resultMap>
	
	
	<!-- 특정 학생의 전체 예약 개수 조회 -->
	<select id="selectListCount" resultType="_int">
		SELECT COUNT(*) AS RESERVATION_COUNT
		  FROM COUNSEL_RESERVATION CR
		  JOIN STUDENT_DETAIL SD 
		    ON CR.STUDENT_NO = SD.STUDENT_NO
		  WHERE SD.USER_NO = #{userNo}
		  AND CR.RESERVATION_STATUS != 'CANCELED'
	</select>
	
	<!-- 특정 학생의 상담 예약 목록 조회 -->
	<select id="selectReservationList" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    CR.STUDENT_NO,
		    CR.CATEGORY_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.INQUIRY_CONTENT,
		    CR.RESERVATION_STATUS,
		    CR.CREATED_AT,
		    CR.UPDATED_AT,
		    CR.COUNSEL_NO,
		    M.USER_NAME AS COUNSELOR_NAME,
		    CC.CATEGORY_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN MEMBER M ON CP.USER_NO = M.USER_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE SD.USER_NO = #{userNo}
		AND CR.RESERVATION_STATUS != 'CANCELED'
		ORDER BY CR.RESERVATION_NO DESC
	</select>
	
	<!-- 전체 상담 카테고리 목록 조회 -->
	<select id="selectCategoryList" resultMap="categoryResultSet">
		SELECT *
		  FROM COUNSEL_CATEGORY
		  ORDER BY CATEGORY_NO ASC
	</select>
	
	<!-- 재직중인 전체 상담사 이름 목록 조회 -->
	<select id="selectCounselorList" resultMap="reservationResultSet">
	    SELECT 
	        CP.COUNSEL_NO,
	        M.USER_NAME AS COUNSELOR_NAME
	    FROM COUNSELOR_PROFILE CP
	    JOIN MEMBER M ON CP.USER_NO = M.USER_NO
	    WHERE CP.STATUS = 'ACTIVE'
	    ORDER BY M.USER_NAME ASC
	</select>
	
	<!-- 새로운 상담 예약 신청 등록 -->
	<insert id="insertReservation" parameterType="reservation">
	    INSERT INTO COUNSEL_RESERVATION (
	        RESERVATION_NO,
	        STUDENT_NO,
	        CATEGORY_NO,
	        CONSULT_DATE,
	        INQUIRY_CONTENT,
	        RESERVATION_STATUS,
	        CREATED_AT,
	        UPDATED_AT,
	        COUNSEL_NO
	    ) VALUES (
	        SEQ_RESERVATION_NO.NEXTVAL,
	        (SELECT STUDENT_NO FROM STUDENT_DETAIL WHERE USER_NO = #{userNo}),
	        #{categoryNo},
	        TO_DATE(#{consultDate}, 'YYYY-MM-DD HH24:MI'),
	        #{inquiryContent},
	        'APPLIED', SYSDATE,
	        SYSDATE,
	        #{counselNo}
	    )
	</insert>
	
	<!-- 상담사 상세 정보 조회 -->
	<select id="selectCounselorDetailList" resultMap="reservationResultSet">
	    SELECT 
	        CP.COUNSEL_NO,
	        CP.CATEGORY_NO,
	        M.USER_NAME AS COUNSELOR_NAME,
	        M.PHONE,
	        M.EMAIL,
	        CC.CATEGORY_NAME
	    FROM COUNSELOR_PROFILE CP
	    JOIN MEMBER M ON CP.USER_NO = M.USER_NO
	    JOIN COUNSEL_CATEGORY CC ON CP.CATEGORY_NO = CC.CATEGORY_NO
	    WHERE CP.STATUS = 'ACTIVE'
	    ORDER BY CP.COUNSEL_NO ASC
	</select>
	
	<!-- 특정 상담사 1명의 요약 정보 및 담당 카테고리 조회 -->
	<select id="selectOneCounselor" parameterType="_int" resultMap="reservationResultSet">
	    SELECT 
	        CP.COUNSEL_NO,
	        CP.CATEGORY_NO,
	        M.USER_NAME AS COUNSELOR_NAME,
	        CC.CATEGORY_NAME
	    FROM COUNSELOR_PROFILE CP
	    JOIN MEMBER M ON CP.USER_NO = M.USER_NO
	    JOIN COUNSEL_CATEGORY CC ON CP.CATEGORY_NO = CC.CATEGORY_NO
	    WHERE CP.COUNSEL_NO = #{counselNo}
	</select>
	 
	 <!-- 특정 예약 건의 상세 내용 조회 -->
	 <select id="selectReservationDetail" parameterType="_int" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    CR.STUDENT_NO,
		    CR.CATEGORY_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.INQUIRY_CONTENT,
		    CR.RESERVATION_STATUS,
		    CR.CREATED_AT,
		    CR.UPDATED_AT,
		    CR.COUNSEL_NO,
		    SM.USER_NAME AS STUDENT_NAME,
		    C.CLASS_NAME,
		    SM.PHONE,
		    SM.EMAIL,
		    CM.USER_NAME AS COUNSELOR_NAME,
		    CC.CATEGORY_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN MEMBER SM ON SD.USER_NO = SM.USER_NO
		LEFT JOIN CLASS C ON SM.CLASS_NO = C.CLASS_NO 
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN MEMBER CM ON CP.USER_NO = CM.USER_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE CR.RESERVATION_NO = #{reservationNo}
	</select>
	
	<!-- 학생 또는 상담사가 예약을 취소할 시 상태값 변경 -->
	<update id="deleteReservation" parameterType="_int">
		UPDATE COUNSEL_RESERVATION
		   SET RESERVATION_STATUS = 'CANCELED',
		       UPDATED_AT = SYSDATE
		 WHERE RESERVATION_NO = #{reservationNo}
	</update>
	
	<!-- 상담 정보 수정 -->
	<update id="updateReservation" parameterType="reservation">
		UPDATE COUNSEL_RESERVATION
		   SET CONSULT_DATE = TO_DATE(#{consultDate}, 'YYYY-MM-DD HH24:MI'),
		       INQUIRY_CONTENT = #{inquiryContent},
		       UPDATED_AT = SYSDATE
		 WHERE RESERVATION_NO = #{reservationNo}
	</update>
	
	<!-- 특정 상담사에게 들어온 전체 예약건수 조회 -->
	<select id="selectCounselorListCount" resultType="_int">
		SELECT COUNT(*)
		  FROM COUNSEL_RESERVATION CR
		  JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		 WHERE CP.USER_NO = #{userNo}
		   AND CR.RESERVATION_STATUS != 'CANCELED'
	</select>
	
	<!-- 특정 상담사 본인에게 신청된 예약 목록 조회 -->
	<select id="selectCounselorReservationList" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.RESERVATION_STATUS,
		    SM.USER_NAME AS STUDENT_NAME,  CC.CATEGORY_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN MEMBER SM ON SD.USER_NO = SM.USER_NO  
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE CP.USER_NO = #{userNo}               
		AND CR.RESERVATION_STATUS != 'CANCELED'
		ORDER BY CR.RESERVATION_NO DESC
 	</select>
 	
 	<!-- 상담사 승인: 예약완료('RESERVED')로 변경 -->
 	<update id="approveReservation" parameterType="_int">
 		UPDATE COUNSEL_RESERVATION
		   SET RESERVATION_STATUS = 'RESERVED',
		       UPDATED_AT = SYSDATE
		 WHERE RESERVATION_NO = #{reservationNo}
 	</update>
 	
 	<!-- 상담사 반려: 상담반려('REJECTED')로 변경 -->
 	<update id="rejectReservation" parameterType="_int">
		UPDATE COUNSEL_RESERVATION
		   SET RESERVATION_STATUS = 'REJECTED',
		       UPDATED_AT = SYSDATE
		 WHERE RESERVATION_NO = #{reservationNo}
	</update>
	
	<!-- 상담 완료 처리: 상담완료('COMPLETED')로 변경 -->
	<update id="completeReservation" parameterType="_int">
		UPDATE COUNSEL_RESERVATION
		   SET RESERVATION_STATUS = 'COMPLETED'
		 WHERE RESERVATION_NO = #{reservationNo}
	</update>
 	
 	<!-- 특정 예약 건에 동반 작성된 상담 일지 내용 조회 -->
 	<select id="selectCounselorLog" parameterType="_int" resultType="counselLog">
		SELECT LOG_NO as logNo,
		       RESERVATION_NO as reservationNo,
		       CONTENT as content,
		       CREATED_AT as createdAt,
		       UPDATED_AT as updatedAt,
		       STATUS
		  FROM COUNSEL_LOG
		 WHERE RESERVATION_NO = #{reservationNo}
		   AND STATUS = 'Y'
	</select>

	<!-- 상담 완료 후 상담 기록 일지 신규 삽입 -->
	<insert id="insertCounselorLog" parameterType="counselLog">
		INSERT INTO COUNSEL_LOG (
			LOG_NO,
			RESERVATION_NO,
			CONTENT,
			CREATED_AT,
			UPDATED_AT,
			STATUS
		) VALUES (
			SEQ_LOG_NO.NEXTVAL, #{reservationNo},
			#{content},
			SYSDATE,
			SYSDATE,
			'Y'
		)
	</insert>
	
	<!-- 이미 작성된 상담일지 내용 수정 -->
	<update id="updateCounselorLog" parameterType="counselLog">
		UPDATE COUNSEL_LOG
		   SET CONTENT = #{content},
		       UPDATED_AT = SYSDATE
		 WHERE RESERVATION_NO = #{reservationNo}
		   AND STATUS = 'Y'
	</update>
	
	<!-- 상담일지 삭제 -->
	<update id="deleteCounselorLog" parameterType="_int">
		UPDATE COUNSEL_LOG
		   SET STATUS = 'N'
		 WHERE RESERVATION_NO = #{reservationNo}
	</update>
	
	<!-- 상담 완료한 상담 이력 카운트 조회 -->
	<select id="selectCounselorCompletedListCount" resultType="_int">
		SELECT COUNT(*)
		  FROM COUNSEL_RESERVATION CR
		  JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		 WHERE CP.USER_NO = #{userNo}
		   AND CR.RESERVATION_STATUS = 'COMPLETED'
	</select>
	
	<!-- 상담사 본인의 완료된 상담 이력 목록 조회 -->
	<select id="selectCounselorCompletedList" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.RESERVATION_STATUS,
		    SM.USER_NAME AS STUDENT_NAME,
		    CC.CATEGORY_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN MEMBER SM ON SD.USER_NO = SM.USER_NO  
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE CP.USER_NO = #{userNo}               
		  AND CR.RESERVATION_STATUS = 'COMPLETED'
		ORDER BY CR.RESERVATION_NO DESC
 	</select>
 	
 	<!-- 학생 본인이 완료한 상담 내역 총 개수 조회 -->
 	<select id="selectStudentCompletedListCount" resultType="_int">
		SELECT COUNT(*)
		  FROM COUNSEL_RESERVATION CR
		  JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		 WHERE SD.USER_NO = #{userNo}
		   AND CR.RESERVATION_STATUS = 'COMPLETED'
	</select>
	
	<!-- 학생 본인이 완료한 과거 상담 리스트 조회 -->
	<select id="selectStudentCompletedList" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.RESERVATION_STATUS,
		    M.USER_NAME AS COUNSELOR_NAME,
		    CC.CATEGORY_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN MEMBER M ON CP.USER_NO = M.USER_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE SD.USER_NO = #{userNo}
		  AND CR.RESERVATION_STATUS = 'COMPLETED'
		ORDER BY CR.RESERVATION_NO DESC
	</select>
	
	<!-- 본인 담당 반 학생들의 예약 개수 카운트-->
	<select id="selectInstructorListCount" resultType="_int">
		SELECT COUNT(*)
		  FROM COUNSEL_RESERVATION CR
		  JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		  JOIN MEMBER M ON SD.USER_NO = M.USER_NO
		 WHERE M.CLASS_NO = #{classNo}
		   AND CR.RESERVATION_STATUS != 'CANCELED'
	</select>
	
	<!-- 담당 반 학생들의 전체 상담 신청 및 전체 현황 출력 -->
	<select id="selectInstructorReservationList" parameterType="_int" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.RESERVATION_STATUS,
		    SM.USER_NAME AS STUDENT_NAME,
		    CM.USER_NAME AS COUNSELOR_NAME,
		    CC.CATEGORY_NAME,
		    C.CLASS_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN MEMBER SM ON SD.USER_NO = SM.USER_NO
		LEFT JOIN CLASS C ON SM.CLASS_NO = C.CLASS_NO
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN MEMBER CM ON CP.USER_NO = CM.USER_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE SM.CLASS_NO = #{classNo}
		  AND CR.RESERVATION_STATUS != 'CANCELED'
		ORDER BY CR.RESERVATION_NO DESC
	</select>
	
	<!-- 전체 예약 현황 총 개수 조회 -->
	<select id="selectAdminListCount" resultType="_int">
		SELECT COUNT(*)
		  FROM COUNSEL_RESERVATION
		 WHERE RESERVATION_STATUS != 'CANCELED'
	</select>
	
	<!-- 모든 학생과 모든 상담사의 예약 신청 내역 출력 -->
	<select id="selectAdminReservationList" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.RESERVATION_STATUS,
		    SM.USER_NAME AS STUDENT_NAME,
		    CM.USER_NAME AS COUNSELOR_NAME,
		    CC.CATEGORY_NAME,
		    C.CLASS_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN MEMBER SM ON SD.USER_NO = SM.USER_NO
		LEFT JOIN CLASS C ON SM.CLASS_NO = C.CLASS_NO
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN MEMBER CM ON CP.USER_NO = CM.USER_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE CR.RESERVATION_STATUS != 'CANCELED'
		ORDER BY CR.RESERVATION_NO DESC
	</select>
	
	<!-- 담당 반 학생들의 완료된 상담 이력 개수 조회 -->
	<select id="selectInstructorCompletedListCount" resultType="_int">
		SELECT COUNT(*)
		  FROM COUNSEL_RESERVATION CR
		  JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		  JOIN MEMBER M ON SD.USER_NO = M.USER_NO
		 WHERE M.CLASS_NO = #{classNo}
		   AND CR.RESERVATION_STATUS = 'COMPLETED'
	</select>
	
	<!-- 담당 반 학생들의 누적된 상담 완료 리스트 명단 조회 -->
	<select id="selectInstructorCompletedList" parameterType="_int" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.RESERVATION_STATUS,
		    SM.USER_NAME AS STUDENT_NAME,
		    CM.USER_NAME AS COUNSELOR_NAME,
		    CC.CATEGORY_NAME,
		    C.CLASS_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN MEMBER SM ON SD.USER_NO = SM.USER_NO
		LEFT JOIN CLASS C ON SM.CLASS_NO = C.CLASS_NO
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN MEMBER CM ON CP.USER_NO = CM.USER_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE SM.CLASS_NO = #{classNo}
		  AND CR.RESERVATION_STATUS = 'COMPLETED'
		ORDER BY CR.RESERVATION_NO DESC
	</select>
	
	<!-- 전체 누적된 '상담완료' 총 이력 건수 -->
	<select id="selectAdminCompletedListCount" resultType="_int">
		SELECT COUNT(*)
		  FROM COUNSEL_RESERVATION
		 WHERE RESERVATION_STATUS = 'COMPLETED'
	</select>
	
	<!-- 축적된 모든 완료 상담 데이터 목록 추출 -->
	<select id="selectAdminCompletedList" resultMap="reservationResultSet">
		SELECT
		    CR.RESERVATION_NO,
		    TO_CHAR(CR.CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS CONSULT_DATE,
		    CR.RESERVATION_STATUS,
		    SM.USER_NAME AS STUDENT_NAME,
		    CM.USER_NAME AS COUNSELOR_NAME,
		    CC.CATEGORY_NAME,
		    C.CLASS_NAME
		FROM COUNSEL_RESERVATION CR
		JOIN STUDENT_DETAIL SD ON CR.STUDENT_NO = SD.STUDENT_NO
		JOIN MEMBER SM ON SD.USER_NO = SM.USER_NO
		LEFT JOIN CLASS C ON SM.CLASS_NO = C.CLASS_NO
		JOIN COUNSELOR_PROFILE CP ON CR.COUNSEL_NO = CP.COUNSEL_NO
		JOIN MEMBER CM ON CP.USER_NO = CM.USER_NO
		JOIN COUNSEL_CATEGORY CC ON CR.CATEGORY_NO = CC.CATEGORY_NO
		WHERE CR.RESERVATION_STATUS = 'COMPLETED'
		ORDER BY CR.RESERVATION_NO DESC
	</select>
	
	<!-- 중복 예약 방지를 위해 특정 상담사의 기존 예약 완료/대기 시간 배열 추출 -->
	<select id="selectReservedDates" parameterType="_int" resultType="string">
		SELECT TO_CHAR(CONSULT_DATE, 'YYYY-MM-DD HH24:MI') AS RESERVED_DATE
		  FROM COUNSEL_RESERVATION
		  WHERE COUNSEL_NO = #{counselNo}
		  AND RESERVATION_STATUS IN ('APPLIED', 'RESERVED', 'COMPLETED')
		  ORDER BY CONSULT_DATE ASC
	</select>
 </mapper>